query GameCatalog($pageSize : Int) {
    allGames(pageSize: $pageSize) {
        results {
            id
            name
            backgroundImage
            tags {
                name
            }
        }
    }
}