query GameCatalog(
    $pageSize : Int,
    $page: Int,
    $search: String
    $genres: String
) {
    allGames(
        pageSize: $pageSize,
        page: $page,
        search: $search
        genres: $genres
    ) {
        nextPage
        results {
            id
            name
            backgroundImage
            thumbnailImage
            tags {
                name
            }
            genres {
                name
            }
        }
    }
}