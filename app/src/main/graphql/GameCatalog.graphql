query GameCatalog($pageSize : Int, $page: Int) {
    allGames(pageSize: $pageSize, page: $page) {
        results {
            id
            name
            backgroundImage
            thumbnailImage
            tags {
                name
            }
        }
    }
}